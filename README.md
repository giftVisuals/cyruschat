# cyruschat (merged into Cyrus-Capital)

The support chat widget now lives inside the main `Cyrus-Capital` repo, at
`/support`. This repo is no longer used — keep deploying from
`Cyrus-Capital` going forward.
